post_install() {
    #mv /etc/pacman.conf /etc/pacman.d/pacman.conf.bak
    #cp /etc/pacman.d/pacman-custom.conf /etc/pacman.conf
}

post_upgrade() {
    #rm /etc/pacman.conf
    #cp /etc/pacman.d/pacman-custom.conf /etc/pacman.conf
}

post_remove() {
    #rm /etc/pacman.conf
    #mv /etc/pacman.d/pacman.conf.bak /etc/pacman.conf
    #echo "restored original pacman file"
}